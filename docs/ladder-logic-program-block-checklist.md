# 梯形图程序段清单

这份文档是 [plc-final-formal-spec.md](/D:/Users/achillesniu/Documents/自研打锅机项目/docs/plc-final-formal-spec.md) 的程序段拆解表。

说明：

1. 这份文档只负责告诉你“程序应该怎么分段”
2. 如果和正式版冲突，一律以正式版为准

---

## 第一组：物理层与安全层

## NW1 物理输入镜像

负责：

1. `X00 ~ X23 -> M300 ~ M319`

## NW2 物理输出镜像

负责：

1. `Y00 ~ Y23 -> M320 ~ M339`

## NW3 心跳监视

负责：

1. 监视 `M400`
2. 心跳超时置 `M574`

## NW4 安全联锁

负责生成：

1. `M570 emergency_stop_active`
2. `M571 global_fatal_block`
3. `M572 heater_block_fault`
4. `M573 water_block_fault`
5. `M575 liquid_low_low`

---

## 第二组：相位执行

## NW5 相位命令识别

负责：

1. 读取 `D300 ~ D315`
2. 识别新相位命令
3. 校验参数

## NW6 相位命令锁存

负责：

1. `D300 ~ D310 -> D500 ~ D509`
2. 回写 `D320`
3. 置 `M501`

## NW7 相位状态跳转

负责：

1. `Idle -> Accepted -> Running`
2. `Running -> Completed / Fault / Stopped`

## NW8 相位 tick 装载

负责：

1. `D502 ~ D508 -> D510 ~ D515`

## NW9 100ms 节拍

负责：

1. 产生统一 `100ms` 脉冲

## NW10 相位 tick 递减

负责：

1. `D510 ~ D515` 递减
2. `D516` 计总运行 tick

## NW11 相位完成位

负责：

1. `M514 ~ M521`

## NW12 水路输出

负责：

1. `D510 > 0 -> Y02`
2. `D511 > 0 -> Y03`
3. `D512 > 0 -> Y04`
4. `D513 > 0 -> Y05`

## NW13 鸡油输出

负责：

1. `D514 > 0 -> Y11`

## NW14 骨膏输出

负责：

1. `D515 > 0 -> Y12`

## NW15 相位完成回写

负责：

1. 回写 `D322 / D323 / D324 / D327`

---

## 第三组：加热

## NW16 加热命令识别

负责：

1. 读取 `D340 ~ D349`
2. 识别 `10 / 11 / 12`

## NW17 加热参数锁存

负责：

1. `D342 -> D530`
2. `D343 -> D531`
3. `D344 -> D532`
4. 计算 `D533 / D534`

## NW18 加热使能 / 关闭

负责：

1. `D341 = 11 -> M535`
2. `D341 = 12 -> 复位 M535/M536`

## NW19 加热回差

负责：

1. `<= 下限` 开
2. `>= 上限` 关
3. 中间保持
4. 禁止条件成立则强制关

## NW20 主备互锁输出

负责：

1. `M536 + D530 = 1 -> Y00`
2. `M536 + D530 = 2 -> Y01`
3. 主备互锁

## NW21 加热状态回写

负责：

1. 回写 `D350 ~ D359`

---

## 第四组：应急补水

## NW22 模式读取

负责：

1. 读取 `D360 / D361`
2. 生成 `M550 / M551 / M552`

## NW23 应急按钮上升沿

负责：

1. `X03 -> M558`

## NW24 点动补水

负责：

1. `Jog` 模式下 `X03 -> Y07`

## NW25 时控补水

负责：

1. `D361 -> D552`
2. `D552` 递减
3. `TimedRunning -> Y07`

## NW26 应急补水状态回写

负责：

1. 回写 `D370 ~ D375`

---

## 建议施工顺序

1. 先做 `NW1 ~ NW4`
2. 再做 `NW5 ~ NW15`
3. 再做 `NW16 ~ NW21`
4. 最后做 `NW22 ~ NW26`

这样最容易先把主链路跑通。
