# Codex Working Rules

本项目是打锅机上位机工程化项目。后续修改必须优先保证长期可维护、可复用、可验证，而不是只做局部补丁。

## 工程化原则

- 优先复用：接单模式、手动模式、设置页、维护页中只要出现相同业务规则，必须先抽公共模型、公共函数或公共组件，再分别做数据适配；不要两边复制同步。
- 发现抽象边界要主动提醒：如果同一规则第二次出现在不同模块，必须主动说明“这里建议抽象”，不要等用户提醒。
- 业务与展示分离：订单解析、配方匹配、相位规划、PLC 通信、UI 展示必须分层；UI 不直接拼复杂业务规则，业务层不直接关心 Compose 展示。
- 避免巨型文件继续膨胀：如果单文件继续变长，优先拆分组件、模型、解析器、协调器；不要把新业务继续堆进 `HomeScreen.kt` 或 `MachineCoordinator.kt`。
- 修改前先查现有链路：改功能前必须先搜索是否已有同类实现、是否已有模型、是否已有文档，不能凭记忆直接补新逻辑。

## 业务硬约定

- 单一事实源：锅底顺序以接口数组顺序为准，不使用 `potSort`；锅底配方匹配以锅型 Code + 锅底 Code 优先，名称只做兜底。
- 手动/接单共享执行核心：手动模式和接单模式可以有不同入口和生命周期，但配方匹配、鸡油/骨膏左上规则、转锅相位规划、锅底详情展示应尽量共用。
- UI 面向操作员：现场 UI 只显示操作员需要的信息，少显示工程解释；锅底详情显示锅底名、口味/备注、底料信息、鲜辅料，不显示加水秒数、出水口、来源、调试语。
- 调试信息归位：PLC 寄存器、Modbus 帧、命令参数、出水口、相位号、加水/鸡油/骨膏时长等工程信息放到设置/维护/调试，不混入首页业务卡片。
- 操作员容错：门店操作员可能会暴力重来，所以取消、暂不处理、重试逻辑要允许恢复，不要把流程设计得必须完美按步骤操作。

## 安全与控制

- 安全优先：加热、急停、液位超低、主备互斥、备用泵互斥这类安全规则必须双层保护，PLC 兜底，安卓也要逻辑自洽。
- 状态以 PLC 为准：上位机可以发命令和显示预期，但设备实际执行状态、故障状态、加热状态、液位状态以 PLC 轮询返回为准。
- 命令幂等：相位命令、加热参数、应急/单独加水等所有写入 PLC 的动作必须有命令号或状态防重策略，避免按钮连点、重启、重试造成重复执行。
- 不破坏现场已验证链路：Modbus 串口配置、帧间隔、IUCLC 修复、03 读寄存器边界、PLC 地址映射这些已验证内容，非必要不改；要改必须先说明风险。

## 文档与验证

- 文档以最终版为准：协议、内存映射、梯形图草案、调试日志如果冲突，以最后确认的 formal/spec/sketch 为准；草案不能反向覆盖正式实现。
- 每次改动后验证：涉及 Kotlin/Android 代码必须至少跑 `:app:assembleDebug`；涉及 PLC 通信必须说明是否做了真实设备验证或仅编译验证。
- 文档类修改不强制编译，但需要说明未编译原因。
